[project]
name = "traducteur-pdf-backend"
version = "0.1.0"
description = "Backend local pour la traduction de documents PDF via Ollama"
requires-python = ">=3.10"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]

[tool.coverage.run]
source = ["app"]
omit = ["app/main.py"]
